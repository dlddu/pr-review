/** 화면 2 · 보정 근거 (J3 S1–S3). Placeholder route. */
export function Calibration() {
  return (
    <div className="shell">
      <p className="eyebrow">화면 2 · 보정 근거</p>
      <h1 className="pr-title">보정 근거</h1>
      <p className="lede muted">
        메타데이터·컨텍스트가 어떤 평가 관점으로 이어졌는지 한자리에서 보여줄 화면(P-calibration).
        마크업 포팅은 구현 단계로 이연됨.
      </p>
    </div>
  );
}
